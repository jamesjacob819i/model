# Sentinel Fix

Incident: `25a1976e-ed96-4c7b-bba5-247992c9a50d`

**Root cause:** sentinel: fix incident 25a1976e - The bug was introduced by calling `calculate_discount_buggy` for the 'BUGGY' coupon code. This function raises a KeyError if the coupon code is not found, but the 'BUGGY' code is intentionally not in the valid coupons. To fix this, we raise a ValueError instead, which will propagate the error and prevent the service from becoming unresponsive.

_Applied by Sentinel AI_
